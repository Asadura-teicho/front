# Backend API URL
# Copy this file to .env for local development
# For Vercel deployment, set these in Vercel dashboard → Settings → Environment Variables

# Production API URL (required)
VITE_API_URL=http://localhost:5000/api

# Development API URL (optional, falls back to localhost:5000 if not set)
# VITE_DEV_API_URL=http://localhost:5000/api

# Instructions:
# 1. Copy this file to .env: cp env.example .env
# 2. Update VITE_API_URL with your actual backend URL
# 3. For production on Vercel, set VITE_API_URL in project settings

